# AU-11 Step 6C-D3 — reservation composite closeout: staging package + execution record (2026-07-27)

**Status:** package **AUTHORED (7D-B DRAFT)**, **NOT committed/pushed**. Design body + owner rulings D-1…D-11 verified 11/10 (Fable). Staging execution progress: **F1→I5 ALL PASS** on staging `pkwotgqivgaapwuqgwqb` (executed by Adam; Claude runs no SQL) — **checkpointed staging execution COMPLETE (Checkpoints A–I all green)**. **POINTER: 7D Step-7 closeout** (canonical status update + commit/push) — owner-gated, NOT yet applied; closeout package prepared for independent review. Production + production attribution NO-GO (attribution gated on D-11). Nothing committed/pushed.

**Governance:** Claude authors + validates; **Adam executes all SQL in staging `pkwotgqivgaapwuqgwqb`** (≠ prod `usayoldrawwmjsmretin`). D3 implementation authorization, staging execution, and production remain NO-GO until separately authorized. Engine hold + Wendy IRA hold + discretionary-transfer deferrals IN FORCE.

## Artifact set (docs/, uncommitted)
1. `phase-au11-6c-d3-staging-schema.sql` — D-9 additive migration (§E2 A–H, resumability catalog assertions).
2. `phase-au11-6c-d3-staging-composite-rpc.sql` — `close_week_with_reservations_v1` (canonical §I 1–19; wrapper capture+verify **before** attribution).
3. `phase-au11-6c-d3-staging-catalog-verify.sql` — column/FK/CHECK/index/RPC-signature/grants-inert/frozen-md5 assertions.
4. `phase-au11-6c-d3-staging-fixture.sql` — ledger-tracked, schema-exact; wk-30 basis + 9 snapshots + batch cd300001 + 7 reservation rows + 10 writer-contract-faithful transactions (owner via subquery, no literal UUID).
5. `phase-au11-6c-d3-staging-fixture-teardown.sql` — asserted, ledger-driven, zero residue incl. attribution.
6. `phase-au11-6c-d3-staging-validation.sql` — ~27 executable isolated tests (sub-block-savepoint model), machine-readable summary, hard-fail.
7. `phase-au11-6c-d3-staging-concurrency.sql` — two-session advisory-lock + uniqueness proof.
8. `phase-au11-6c-d3-staging-rollback.sql` — §8A bare-drop, fail-closed on non-fixture attribution.
9. `phase-au11-6c-d3-staging-post-rollback-verify.sql` — D1/D2/frozen intact; D3 absent.
10. `phase-au11-6c-d3-staging-final-state-verify.sql` — mechanically-enforced Checkpoint I (incl. S3(b); owner email-anchored, no UUID).
11. `phase-au11-6c-d3-execution-record-2026-07-27.md` — this record.

## Frozen / untouched
`save_weekly_closeout_with_snapshots` (md5 `e2a112b376dc32c43e1615e4a4abf24a`), `save_reconciliation_with_commitments` (`1bfde751…`), `save_goal_funding_snapshots` (`154231b3…`), `repair_commitments_for_week`, `getCashAvailabilityEngine`, `isReservedAsOf`, `runModel`, D1/D2 artifacts, client code, `CODEX_STATUS.md`. D3 wraps the closeout; it never edits it.

## Execution evidence
- **7D-F2 (checkpoint A · schema) = PASS (2026-07-28, staging `pkwotgqivgaapwuqgwqb`, executed by Adam).** Applied `…-schema.sql` A–H inside one `BEGIN…COMMIT`; STEP G NOTICE fired (attribution invariant 0/0/0; fk_valid/chk_valid/index_present all true). Post-application catalog verification: CHK_column, CHK_fk, CHK_index, CHK_attribution_invariant, CHK_d1_shape_unchanged, CHK_frozen_wrapper_md5 = PASS. CHK_check initially reported FAIL due to PostgreSQL canonicalization of the stored CHECK (`::text` casts + parens); the constraint is catalog-correct and validated — CHK_check_corrected = PASS.
  - **Two verifier/resumability false-negatives fixed 2026-07-28** (verifier robustness only; NO DDL/semantic change; applied schema unaffected): (B) `…-schema.sql` STEP B FK resumability now catalog-based (`confrelid`/`conkey`/`confkey`/`confupdtype`/`confdeltype`/`condeferrable`) instead of `REFERENCES public.transactions(id)` text; (C) `…-schema.sql` STEP C + `…-catalog-verify.sql` CHK_check + `…-final-state-verify.sql` PART-1 CHECK assertion now normalize canonical output (strip `::text` + parens + whitespace). Re-review: Code static PASS; Fable read-only re-review of the 3 corrected artifacts required before their next use (G-2 / I-4 / I-5).

- **7D-G1 (checkpoint B · composite RPC) = PASS (2026-07-28, staging, executed by Adam).** Applied the unchanged reviewed `…-composite-rpc.sql` (sha256 `7610f61e…`) inside one `BEGIN…COMMIT` with a fail-closed pre-guard (staging + current_user=postgres + D-9 schema present/valid + frozen md5) and an in-transaction post-create assertion (exactly one function; owner=postgres; SECURITY DEFINER; search_path=''; return jsonb; exact 14-arg signature; anon/authenticated EXECUTE=false). Result: "Success. No rows returned" (G1 PASS notice fired, COMMIT). RPC resting-inert.

- **7D-G2 (checkpoint C · catalog verify) — first attempt = VERIFIER EXECUTION ERROR (not a schema/DB failure), 2026-07-28.** SQLSTATE 42725 "operator is not unique: char || unknown" on CHK_fk's `actual` display expression — `pg_constraint.confupdtype`/`confdeltype` are internal `"char"` and require explicit `::text` for concatenation. Read-only; NO mutation; G2 neither PASS nor FAIL. Correction (display-only; PASS/FAIL logic untouched): added `::text` casts on `confupdtype`/`confdeltype` (line 16) and the sibling `provolatile`/`proparallel` in CHK_rpc (line 49). Sweep across all D3 artifacts confirmed these were the only two uncast `"char"` concatenations. `catalog-verify.sql` hash `388977fd…` → `2b89dfd3…` (full `2b89dfd3106bd153426644214a67b4eca1e67f6a595cea7493c0910cbc8d13b3`); all other artifacts unchanged. Code static PASS; focused Fable re-review **PASS** (display-only ::text casts; no PASS/FAIL logic change; no residual uncast char-concat).
- **7D-G2 (checkpoint C · catalog verify) = PASS (2026-07-28, staging, executed by Adam).** Ran the corrected checks as a single-result-set `UNION ALL` presentation (PASS/FAIL predicates byte-identical to `catalog-verify.sql` `2b89dfd3…`; reshape did not modify the artifact). All 8 rows PASS: CHK_column (`uuid/YES/NULL`), CHK_fk (`c/r/def=false/valid=true`), CHK_check (`true`), CHK_index (unique partial), CHK_d1_shape_unchanged (`true`), CHK_rpc (exact 14-arg sig, secdef, owner=postgres, empty search_path, jsonb), CHK_rpc_grants_inert (`anon=false authenticated=false`), CHK_frozen_wrapper_md5 (`e2a112b376…`). The earlier 42725 was a read-only verifier execution defect — **not** a schema/RPC failure.
- **NEXT POINTER: 7D-H1 (fixture install)** — not issued (stopped for the night after G2).

- **7D-H1 (checkpoint D · fixture) — HARDENED before first execution (2026-07-28), not yet run.** Owner caught a teardown-safety defect: the fixture used `ON CONFLICT DO NOTHING` for `weekly_reconciliations` wk30 and `goal_funding_snapshots` (2026/30) for NINE REAL goal IDs with broad ledger keys → silent reuse of pre-existing legit rows + false ownership. Teardown analysis: teardown is namespaced (recon by sentinel `chk=3030.30`; snapshots by `note LIKE '[STAGING-FIXTURE]%'`), so it does NOT delete non-fixture rows; the real risk was the inverse (silent reuse) + a negligible sentinel-collision edge. Fix: fixture is now **fail-closed on any pre-existing key** (guard RAISEs if any wk30 recon, any designated 2026/30 snapshot, any d3fix goal/commitment, batch cd300001, [STAGING-FIXTURE] txn, or non-empty ledger exists), all INSERTs plain (no ON CONFLICT), each proven created-by-this-run via ROW_COUNT (goals=7, recon=1, snapshots=9, commitments=7, txns=10; batch via RETURNING), **per-row ledger ownership** (35 records incl. snapshots `'2026:30:<goal_id>'`), and post-insert count assertions. Teardown UNCHANGED (`80555479…`, already exactly fixture-scoped). Focused Fable re-review = **PASS 7/7** (fail-closed guard complete, no ON CONFLICT, creation proven, 35 per-row ledger, teardown exactly fixture-scoped with no non-fixture-deletion path, schema-exact, H2-compatible). Applied Fable's one recommended symmetry fix: goal guard widened `= ANY(c_fix_goals)` → `id LIKE 'd3fix_%'` (matches teardown + commitments guard). Fixture hash `7982fc18…`→`82f76ac5…`→**`45fca623…`** (full `45fca6231a77f160d21ba559a0bc13d8a4e57cd937a4be84e1f8f9f0b51d6031`). Teardown UNCHANGED `80555479…`. Code static PASS. H1 reissued (hardened) with the expanded pre-check gate.

- **7D-H2 (checkpoint E · validation) — HARDENED before first execution (2026-07-28), not yet run.** Pre-execution finding: the harness sets a TRANSACTION-LOCAL `role=authenticated` inside the DO block (does not revert at DO exit); under one-transaction execution (Supabase editor) it would leak and make the post-block `REVOKE`/`DROP` (privileged) fail → grant discipline broken. Fix: added `RESET ROLE;` between the DO block and the REVOKE (line 275; no-op under autocommit, correct under one-tx). No test logic changed. Fable re-review then caught a SECOND, latent defect (blocking): four tests do direct pre-condition UPDATEs on cash_commitments/discretionary_reservation_batches while role=authenticated, but authenticated is SELECT-only on those tables (5F-1 lines 219-224; D1 lines 172-173 — mutations-through-RPCs-only) → 42501 fails T-REPLAY-B-SAME/T-OUT-OF-BATCH/T-REPLAY-C-DIFF/T-STATE-F-CHECK → hard-fail. Fix (Fable's direction; NOT granting UPDATE to authenticated): elevate to owner (`set_config('role','postgres',true)`) for the setup UPDATEs only, restore authenticated before each RPC call so authz is still exercised (T-STATE-F-CHECK has no RPC → sub-block rollback reverts). Validation hash `23337c7d…`→`7961adfc…` (RESET ROLE)→`b695be4d…` (role-elevation) (full `b695be4d2cce26ffd20d50fb22b87b3002e3d75262b7533147f0a985b5537b8b`). Code static PASS; 2nd focused Fable re-review = **PASS 6/6** (42501 resolved for all four; authz still exercised as authenticated; T-STATE-F-CHECK reaches 23514 CHECK as owner; role never leaks between tests; grant discipline + isolation intact; zero residue). Owner then caught a THIRD latent defect (blocking): `pg_temp.d3_rec` was SECURITY INVOKER → its `INSERT INTO public.d3_test_results` would run as authenticated and 42501 if authenticated lacks INSERT (do not rely on Supabase default privileges unprovento). Fix (owner option a): `d3_rec` now SECURITY DEFINER (owner postgres) + `SET search_path=''` + fully-qualified refs → INSERT/residue run as owner, granting authenticated NOTHING on any table. Added (#1) a read-only privilege preflight meta-row `Z-PRIV-auth-insert-on-results` (records `has_table_privilege('authenticated','public.d3_test_results','INSERT')`) and (#5) a recorder self-test `T-RECORDER-SELFTEST` (uncaught d3_rec call before the 31 tests → hard-stops if the recorder path is broken under role=authenticated). Validation hash `23337c7d…`→`7961adfc…`(RESET ROLE)→`b695be4d…`(role-elevation)→`fd3da4d5…`(SECURITY DEFINER recorder) (full `fd3da4d51fb3794cce4b90227a36863af7f56fa6cd411b9eaef92d98741d3341`). Summary now 33 rows (1 meta + 1 self-test + 31 tests). 3rd Fable = PASS 7/7 (non-blocking note: summary didn't assert row count). Owner then required TWO machine-enforcement hardenings: (#1) summary asserts exactly 33 result rows (hard-fail on partial table); (#2) three machine-asserted grant-window states via `$g0$` (authenticated EXECUTE absent BEFORE grant — catches a pre-existing leak the REVOKE would otherwise silently repair), `$g1$` (present during), `$g2$` (absent after REVOKE) — displays kept, assertions added. Validation hash `fd3da4d5…`→`b2cc70c8…` (full `b2cc70c87e5fd57ff58c256dad71f404da70a410d20455c9bcb1ca0acb51e9c2`). 4th Fable = PASS 5/5. Owner then caught a fail-safety gap: under autocommit a committed GRANT with a never-reached REVOKE (on an unexpected error between them) leaves the RPC exposed. Fix: wrap the ENTIRE harness in one explicit `BEGIN…COMMIT` so the temporary EXECUTE grant is atomic — any error rolls back GRANT + all mutations; COMMIT only after the grant-state/33-row/all-PASS/zero-residue gates pass; RESET ROLE (now always-active) precedes the privileged REVOKE/DROP; standalone inert check runs post-COMMIT. Validation hash `b2cc70c8…`→`ff0144dc…` (full `ff0144dc67be57375dad61c50e60aee40ddcb3b4046c3378b50d602de279e736`). Code static PASS; 5th focused Fable re-review launched (atomic grant rollback under failure); H2 re-issue held pending PASS.

## Checkpoint scaffold (to be filled at execution — Adam)
- **A schema** — DONE: 7D-F2 PASS (see Execution evidence above).
- **B composite RPC** — DONE: 7D-G1 PASS (see Execution evidence above).
- **B composite RPC** — apply `…-composite-rpc.sql`. ‹evidence pending›
- **C catalog-verify** — DONE: 7D-G2 PASS (all 8 CHK_* PASS; see Execution evidence above).
- **D fixture** — DONE: **7D-H1 PASS (2026-07-28)** — hardened fixture `45fca623…` executed (pre-H1 reconfirmation passed); FIX_CHECK = reservation_rows 7 / fixture_txns 10 / active_batch 1 / snapshots 9 / goals 7 / recon 1 / ledger_records 35. Executed version = `45fca6231a77f160d21ba559a0bc13d8a4e57cd937a4be84e1f8f9f0b51d6031`.
- **E validation** — DONE: **7D-H2 PASS (2026-07-28)** — hardened harness `ff0144dc…` executed; **exactly 33 result rows, all PASS, residue 0**; standalone post-COMMIT inert check anon=false/authenticated=false/inert=true. Executed version = `ff0144dc67be57375dad61c50e60aee40ddcb3b4046c3378b50d602de279e736`. Reached 11/10 over 5 hardening rounds (RESET ROLE grant-discipline; role-elevation for 4 direct-UPDATE tests; SECURITY DEFINER recorder; machine-asserted 33-row + 3 grant-window states; atomic BEGIN…COMMIT fail-safe grant) — all Fable-verified.
- **F concurrency** — DONE: **7D-H3 PASS (2026-07-28, two direct/session psql connections, PIDs 185152/185162)** — preflight can_execute_rpc=true/all_staging=true both; A1 lock held; **B1 SQLSTATE 55P03** (pg_advisory_xact_lock lock timeout); A2/B1 rollbacks ok; **B2 duplicate_transaction_nomination**; V residue 0/0. Executed version = concurrency `1bc828dc56dbbf7362e3f7179f24c9fbecb36887ecc61b383648a1690e7fe450`. HARDENED before execution (2026-07-28): as-written it set `role=authenticated` but the RPC is resting-inert post-H2 → B1/B2 would 42501 (EXECUTE denied). Fix: OWNER/SERVICE-session mode — removed the 3 `role=authenticated` lines; `can_write_financials()` keys on `auth.uid()` (jwt=owner, per phase-5a-role-enforcement.sql:43-53) not session role, so the owner login role passes authz and EXECUTEs the inert RPC with NO grant (no grant-exposure window). Advisory-lock + unique-index proofs are role-independent. Concurrency hash `2689dd31…`→`848a2397…` (full `848a239789f614ac0caba33568cbcb25d0987bf6cb8135cb7dcc1c92162e8d20`). Code static PASS; focused Fable re-review = PASS 6/6. Expected PASS-A1 / B1(55P03) / A2 / B2(duplicate_transaction_nomination) / V(zero residue). RUNBOOK correction (no artifact change; hash stays `848a2397…`): added a per-terminal preflight (current_user/session_user/has_function_privilege(...,'EXECUTE')=true/staging) and split each error block's ROLLBACK into a SEPARATE operator action, because under psql ON_ERROR_STOP the trailing ROLLBACK in a pasted error block does not run (would strand an aborted txn). Ordering preserved: A1 → B1 error → B1 rollback → A2 rollback → B2 error → B2 rollback → V. FIRST EXECUTION (Supabase editor, single-session) surfaced two findings: (a) the editor runs each execution as a separate connection so A1's advisory lock was not held across B1 → no contention; (b) B1's single-r1 payload, when uncontended, sailed past §I.6 (advisory lock, line 122) to §I.16 (omitted_cleared_row, line 246) and raised P0001 omitted_cleared_row (control-flow order is CORRECT: lock §I.6 precedes omitted §I.16 — the error was purely the missing contention). Payload corrected: B1 now retires the FULL eligible set (r1 auto + r3 nominated MULTI_A + r4 auto) so its only correctly-run failure is the lock (55P03); an accidental single-session run returns ok=true (clean "redo two-session" signal), never omitted_cleared_row. Concurrency hash `848a2397…`→`1bc828dc…` (full `1bc828dc56dbbf7362e3f7179f24c9fbecb36887ecc61b383648a1690e7fe450`). Code static PASS; Fable re-review launched; H3 held. Reissue requires TWO concurrent psql sessions (session/direct connection, not the editor/transaction-pooler).
- **7D-I1 (fixture teardown)** — HARDENED before execution (2026-07-28): the prior teardown blind-`DELETE`d then `DROP`ped the ledger and relied on namespace patterns. Owner required ledger-ownership-driven teardown. Rewrote it to (1) assert ledger exists, (2) exactly 35 records, (3) allowed kinds + exact per-kind counts (7/1/9/1/7/10), (4) non-null keys (PK-unique), (5) every ledger ref resolves to a fixture object, (6) every namespaced fixture object is ledgered (namespace-exclusivity), (7) delete through ledger-owned refs, (8) exact delete-count asserts (7/10/1/9/1/7), (9) empty ledger only after owned objects gone (assert 35 removed) then drop, all atomic + fail-closed. Teardown hash `80555479…`→`84a85853…` (full `84a858534bc2558262039f5f192ea5f05b8adf71d43546a2d7b1c022b0e5a97a`). Code static PASS; focused Fable re-review launched; I1 issue held pending PASS.
  - **7D-I1 = PASS (2026-07-28, teardown `84a85853…`).** TD_residue all seven zeros; a separate read-only I1_POST_VERIFY returned environment_failure 0 / reservation_rows 0 / fixture_txns 0 / batch 0 / snapshots 0 / reconciliation 0 / goals 0 / ledger_present 0 / dangling_attributions 0 / result PASS. **EVIDENCE-DESIGN LESSON:** the Supabase SQL editor SUPPRESSES `RAISE NOTICE` output — durable PASS evidence must be returned via SELECT result sets, not NOTICE. Apply to I2/I3/I4/I5: each checkpoint's PASS must surface in a final SELECT (add a post-verify SELECT where the artifact currently signals via NOTICE only).
- **7D-I2 (rollback) — HARDENED before execution (2026-07-28):** owner flagged two gaps — (1) the pg_constraint/pg_class absence checks were name-only (could false-detect same-named objects in another schema/table), (2) the boundary claimed D1 shape CHECK intact but didn't certify it. Fix: all D3-absence checks now SCHEMA/TABLE-QUALIFIED (constraints via pg_class/pg_namespace to public.cash_commitments; index via pg_index/indrelid to public.cash_commitments relkind='i'), and added D1-invariant certification — exactly one VALIDATED chk_au11_reservation_shape on public.cash_commitments with the pinned normalized def (reused verbatim from the D2-proven canon, normalization lower+strip::text+strip parens/ws) + drift RAISE; RB_absence + I2_POST_VERIFY now return D3-absent AND d1_shape_valid=1 AND result. Rollback hash `590ca693…`→`30fd20ac…` (full `30fd20acf616bf6f5ee61f8665c8f98f6c40a72e2459e1614185bf0c7afedfa6`). Code static PASS; focused Fable re-review launched; I2 issue held pending PASS.
  - **7D-I2 = PASS (2026-07-28, rollback `30fd20ac…`).** RB_absence col/fk/chk/idx/fn 0, d1_shape_valid 1, result PASS; I2_POST_VERIFY staging_ok true / d3 all 0 / d1_shape_valid 1 / d1_def_ok true / result PASS. D-9 schema + composite RPC removed; D1 shape CHECK intact.
- **7D-I3 (post-rollback-verify) — HARDENED before execution (2026-07-28):** applied both prior lessons proactively — (a) it was FOUR separate SELECTs (editor shows only the last → PRV_d3_absent/d1/d2 invisible) → folded into ONE UNION ALL result set (5 rows incl. PRV_OVERALL, each with a result); (b) name-only pg_class/pg_constraint checks (D3 index/constraints, D1 indexes) → all SCHEMA/TABLE-QUALIFIED. Also upgraded: D1 shape CHECK normalized-def match (D2 canon, I2 parity), D2 RPCs by EXACT to_regprocedure signature, frozen RPCs by pinned md5 (wrapper e2a112b3/recon 1bfde751/snapshot 154231b3) with ABSENT guard. Read-only, no mutation. Hash `643849f7…`→`92646342…`. Fable PASS 7/7, then owner caught a final gap: the D3 RPC absence was still schema+proname-only (would false-fail on a legit overload; doesn't prove the exact dropped signature is gone). Fix: `rpc_absent = to_regprocedure('public.close_week_with_reservations_v1(integer,integer,numeric,numeric,numeric,numeric,numeric,text,jsonb,jsonb,jsonb,text,integer,jsonb)') IS NULL`, used in PRV_d3_absent + PRV_OVERALL; renamed PRV_d2_intact→PRV_d2_signatures_present (labels what's actually certified: exact-signature presence, not body/grant integrity). Hash `92646342…`→`fd18e674…` (full `fd18e6747ebb0fea6f66a0c9cd6201b3f25126d43a4fa2d386f842dc9e7111f0`). Code static PASS; focused Fable re-review launched; I3 issue held pending PASS.
  - **7D-I3 = PASS (2026-07-28, post-rollback-verify `fd18e674…`).** All 5 rows PASS: PRV_d1_intact (5 cols/shape_valid 1/def_ok true/both indexes 1), PRV_d2_signatures_present (create/mark/void true), PRV_d3_absent (col/cons/idx 0, rpc_absent true via exact 14-arg sig), PRV_frozen_intact (wrapper e2a112b3/recon 1bfde751/snapshot 154231b3), PRV_OVERALL staging true → PASS. Rollback→post-rollback cycle proven.
- **G rollback + post-rollback** — DONE: I2 + I3 PASS (see Execution evidence).
- **7D-I4 (clean reapply) — HARDENED before execution (2026-07-28):** owner required I4b be fully self-contained (paste full RPC, not "reuse earlier block") + several exact-object certifications. Applied: schema STEP G index-presence now schema/table-qualified pg_index join (schema `be095b52…`→`259d50f9…`); catalog-verify rewritten to ONE result set with CHK_index qualified, CHK_rpc + CHK_rpc_grants_inert via exact 14-arg to_regprocedure OID, CHK_frozen_md5 via exact to_regprocedure sigs, CHK_check_def vs PINNED normalized D3 canon (exact, not substring), CHK_d1_shape_def vs PINNED D1 canon (not convalidated-only), + CHK_OVERALL/staging gate, NULL-oid-safe grants (catalog-verify `2b89dfd3…`→`0a8bc302…`); composite-rpc UNCHANGED `7610f61e…`. I4a durable post-check strengthened to prove uuid/YES/NULL. Fable PASS 7/7. Owner then caught two exact-object regressions in I4b's apply harness: (a) frozen-wrapper pre-guard used a schema+proname scalar subquery (overload-sensitive, md5 of NULL→NULL not fail-closed); (b) post-assert used a schema+proname count=1 (false-fails on a legit overload). Fix (folded into composite-rpc.sql so it is the self-contained, hash-traced apply — RPC body byte-identical): pre-guard resolves the frozen wrapper by EXACT 13-arg to_regprocedure, RAISE if NULL, then md5 IS DISTINCT FROM pinned; post-assert resolves the EXACT 14-arg to_regprocedure OID, RAISE if NULL, inspects by p.oid (sig/owner/secdef/empty search_path/jsonb/VOLATILE/PARALLEL-UNSAFE/inert grants) — count check removed. Composite-rpc `7610f61e…`→`5c8fbb15…` (full `5c8fbb1528f14e4e72c501e035ddaec131e4bb2365cf6e503d6bf35b39b22fad`). Code static PASS; focused Fable re-review PASS 5/5 (frozen pre-guard exact-13-arg to_regprocedure + RAISE-if-NULL + md5 IS DISTINCT FROM; post-assert exact-14-arg OID + RAISE-if-NULL + by-oid inspect, no count; body byte-identity PROVEN by reconstructing 7610f61e from edit history and byte-diff; atomic BEGIN..COMMIT). Complete self-contained I4 (a/b/c) reissued 2026-07-27.
- **H clean reapply** — DONE: **7D-I4 = PASS (2026-07-28, staging, executed by Adam).** Hash-verified before execution (schema `259d50f9…`, post-check `dc2cb329…`, composite-rpc `5c8fbb15…`, catalog-verify `0a8bc302…`). Sequence: **I4a schema apply** → "Success. No rows returned." → **I4a durable post-check** (`phase-au11-6c-d3-staging-schema-postcheck.sql` `dc2cb329449522a96414f919e32d028c8d6d0a9567d5e0c16b69dd330b436c04`, added this checkpoint to restore durable catalog evidence after the STEP-G NOTICE proved editor-suppressed) → **col_uuid_yes_null=1, fk_valid=1, chk_valid=1, idx=1, result=PASS** → **I4b composite-RPC apply** (self-contained `5c8fbb15…`) → "Success. No rows returned." → **I4c catalog verify** → **9/9 PASS** (CHK_column, CHK_fk, CHK_check_def, CHK_index, CHK_d1_shape_def, CHK_rpc, CHK_rpc_grants_inert, CHK_frozen_md5, CHK_OVERALL; staging=true). Rollback→reapply→verify cycle proven idempotent and clean. Per-artifact control note (accurate): only I4b enforces current_user=postgres; all three enforce the staging gate; I4c also enforces current_user only via staging/catalog state (no current_user check). Executed artifact hashes recorded above.
- **I final-state** — DONE: **7D-I5 = PASS (2026-07-28, staging `pkwotgqivgaapwuqgwqb`, executed by Adam; hash-verified `efd2a029…` before run).** PART 1 raised nothing; PART 2 returned **12/12 PASS**: FS_column {n:1,uuid,YES,default_null:true}; FS_fk_valid {n:1,upd:c,del:r,deferrable:false,valid:true,ref_ok:true}; FS_check_valid {n:1}; FS_index {n:1,am:btree,uniq:true,natts:1,nkeyatts:1,key_col:cleared_transaction_id,predicate:"(cleared_transaction_id IS NOT NULL)",indexdef:"CREATE UNIQUE INDEX uix_au11_cleared_txn ON public.cash_commitments USING btree (cleared_transaction_id) WHERE (cleared_transaction_id IS NOT NULL)"}; FS_d1_shape {n:1}; FS_rpc {prosecdef:true,owner:postgres,empty_sp:true,ret:jsonb,args:"integer, integer, numeric, numeric, numeric, numeric, numeric, text, jsonb, jsonb, jsonb, text, integer, jsonb",vol:v,par:u}; FS_rpc_inert {anon_exec:false,auth_exec:false}; FS_frozen_md5 {wrapper e2a112b376…,recon 1bfde751…,snapshot 154231b3…}; FS_fixture_absent {res_resid:0,txn_resid:0,attr_resid:0,ledger_absent:true}; FS_s3b_invariant {violations:0}; FS_owner_integrity {active_owners:1,qualifying_owners:1}; FS_OVERALL {staging:true}. **CHECKPOINT I COMPLETE.** Executed version = final-state-verify `efd2a0293fec24e49a996984fe99c396cebc31cde66904743d763e30a691a3ed`.
- **(superseded draft note)** 7D-I5 hardened + issued for independent review (now executed PASS above). `…-final-state-verify.sql` `2cc9daa8…`→`0fc5af80…` (full `0fc5af80df186fa30de62a129464c055d0bd8d872a1cfb5573a26d4e052594ba`) (exact-object hardening: name-only index/RPC/constraint/frozen-md5 lookups → schema/table-qualified + exact-signature 14-arg RPC OID / 13-arg wrapper to_regprocedure + fail-closed `IS DISTINCT FROM` md5; PART-2/PART-3 folded into ONE editor-visible 12-row UNION-ALL result set ending FS_OVERALL). Code static PASS + Fable PASS 7/7 at `0fc5af80…`. Owner then ruled on the two open items and added two blockers: (1) relax owner-integrity to EMAIL-AGNOSTIC (exactly one active auth-backed owner; no email pinned/exposed); (2) column-dependency behavior kept (raw SQL error = hard-fail evidence, no masking); (3) BLOCKER — index verification was substring ILIKE (accepts extra key/INCLUDE cols or drifted expr) → replaced with EXACT STRUCTURAL proof (public.cash_commitments, btree, indisunique, indnatts=1 AND indnkeyatts=1, indexprs IS NULL, key attname=cleared_transaction_id, predicate normalized-exact = cleared_transaction_idisnotnull) in BOTH PART 1 and PART 2; (4) BLOCKER — frozen md5 coverage regressed to wrapper-only → restored ALL THREE (wrapper e2a112b3/recon 1bfde751/snapshot 154231b3) via I4c exact-signature to_regprocedure inline pattern, fail-closed (RAISE-if-NULL in PART 1; 'ABSENT' sentinel in PART 2), all three reported in FS_frozen_md5, passes only when all three resolve + match. final-state-verify `0fc5af80…`→`353aba26…` (full `353aba265f474289052eed4cd2e4599f123abb7dfd611f9ec6cb76b7e322422d`). Code static PASS (read-only; 0 ILIKE in code; email pin absent; three frozen pins/sigs present; 12 rows). Focused Fable re-review = **PASS 4/4**: (1) email-agnostic — pure count gate, no email in executable SQL/result set, no email_backed; (2) index proven structurally in BOTH parts (rejects extra key col via indnkeyatts=1, INCLUDE via indnatts=1, expression via indexprs IS NULL [double-rejected: expr key ⇒ indkey[0]=0 joins no pg_attribute row], predicate drift, non-btree; indkey[0] correct 0-based int2vector; NOT attisdropped sound/safe-direction); (3) all three frozen exact-sig+md5, byte-identical to catalog-verify fz (programmatically verified — sigs 3×/pins 2× in final-state vs 2×/1× in catalog-verify, identical), PART 2 inline CASE pattern, no proname/nullable-subquery, 'ABSENT' forces FAIL; (4) no regression — D3/D1 canons byte-identical PART1↔PART2↔catalog-verify, 12 rows ord 1..12 ending FS_OVERALL (ANDs all incl staging), FS_frozen_md5=row 8 / FS_owner_integrity=row 11, strictly read-only (one BEGIN = DO-opener). Non-blocking notes: RPC-absent edge stays fail-closed via FS_rpc+FS_OVERALL+PART1 abort; `condeferrable AS deferrable` valid. final-state-verify reached `353aba26…`, Fable PASS 4/4. Owner then caught a remaining owner-integrity gap: `qualifying_owners=1` alone silently EXCLUDES a malformed second active owner (NULL auth_user_id or no auth.users backing), so it cannot prove EXACTLY ONE active owner (false-PASS: one good owner + one malformed active owner). Fix (both PART 1 and PART 2): two-count invariant — `active_owners` (role='owner' AND active) AND `qualifying_owners` (…+auth-backed); FS_owner_integrity PASS iff active_owners=1 AND qualifying_owners=1 (qualifying ⊆ active ⇒ the sole active owner is the backed one). Rejects: 2nd malformed active owner (active=2), zero active (active=0), two valid active (active=2,qual=2); passes only one active auth-backed (1/1). PART 1 RAISEs on the same two-count invariant so PART1↔PART2 cannot disagree. Email-agnostic, no email/UUID exposed. final-state-verify `353aba26…`→`efd2a029…` (full `efd2a0293fec24e49a996984fe99c396cebc31cde66904743d763e30a691a3ed`). Code static PASS; focused Fable re-review = **PASS** with 5-scenario truth table: (1) one active auth-backed → PASS (a=1,q=1); (2) good + malformed-second-active → REJECTED (a=2 — the prior hole, now closed for both NULL-id and dangling-id variants); (3) zero active → REJECTED (a=0); (4) two valid active → REJECTED (a=2,q=2); (5) one active but unbacked → REJECTED (a=1,q=0). PART1 raise `a<>1 OR q<>1` is exact De Morgan of PART2 `a=1 AND q=1`; count(*) never NULL ⇒ no divergence; both parts byte-identical predicates. Email-agnostic confirmed (no @/UUID/email_backed in executable SQL; detail=to_jsonb(own) = two counts only); auth.users.id PK makes EXISTS ⟺ exactly-one-backing (sound). No regression elsewhere; 12 rows, FS_owner_integrity=row 11, FS_OVERALL ANDs owner_ok. **7D-I5 READY FOR EXECUTION; held pending owner go.** final-state-verify `efd2a029…` (full `efd2a0293fec24e49a996984fe99c396cebc31cde66904743d763e30a691a3ed`).

## Confirmations
No production; no SQL executed by Claude; no client wiring; the composite is dormant (inert grants); frozen surfaces untouched; operational holds (incl. Wendy IRA) unchanged. D3 implementation / staging execution / production all NO-GO pending separate explicit authorization; production attribution additionally gated on the D-11 Register edit/delete protection.
